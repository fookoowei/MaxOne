# --- Postgres credentials ---
# Consumed by docker-compose.yml via ${...} substitution (creates the DB user/db).
POSTGRES_USER="user"
POSTGRES_PASSWORD="password"
POSTGRES_DB="wallet_db"

# --- Database connection string ---
# Consumed by the app / Prisma. Must stay consistent with the POSTGRES_* values above:
#   postgresql://<POSTGRES_USER>:<POSTGRES_PASSWORD>@localhost:5432/<POSTGRES_DB>
DATABASE_URL="postgresql://user:password@localhost:5432/wallet_db"

# --- Auth secrets (used later, in the authentication milestone) ---
JWT_ACCESS_SECRET="change-me"
JWT_REFRESH_SECRET="change-me"

# --- App server port ---
# Read by main.ts (process.env.PORT ?? 3000). Pick a port that avoids collisions
# (we use 3100); keep it consistent with frontend API_BASE_URL.
PORT=3100

# M16a: Redis cache (db 0 = app, db 1 = integration tests — flushed per test)
REDIS_URL="redis://localhost:6379/0"
TEST_REDIS_URL="redis://localhost:6379/1"
